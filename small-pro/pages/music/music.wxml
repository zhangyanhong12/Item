<view class="item" wx:for="{{music}}" wx:for-item="list" wx:key="list" >
    <view class="title">
        <text>{{list.title}}</text>
        <text class="more" data-url="{{list.subTitle}}"
        data-title="{{list.title}}" bind:tap="handleClick">更多></text>
    </view>
  <view class="flex" >
    <music wx:for="{{list.data}}" data="{{item}}"></music>
    </view>
</view>
